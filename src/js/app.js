/*
//forEach function with spread operator
const abcde = [...document.querySelectorAll('[data-name]')]
abcde.forEach(function (el){
    //actions per element
});
*/

window.App = {
    update : () =>{
        //update functions
    }
};

window.addEventListener('DOMContentLoaded', () => {
    window.App.update();
});